/* 

export function getNodes(array, parentNode) {
    console.log("hi");
    const children = parentNode.children;
    console.log(parentNode);
    if (children) {
        for (const child of children) {
            getNodes(array, child);
        }
    }
} */